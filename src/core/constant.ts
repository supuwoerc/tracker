export enum TMessageType {
    error = 'error',
    request = 'request',
    crash = 'crash',
    performance = 'performance',
    customer = 'customer',
}

export enum TVitalsType {
    fcp = 'fcp',
    lcp = 'lcp',
    cls = 'cls',
    inp = 'inp',
}

export enum TRequestType {
    XMLHttpRequest = 'XMLHttpRequest',
}
